<% content_for :meta_title, "#{@garden.title} - Jouw Stek" %>
<% content_for :meta_description, @garden.description %>
<% content_for :meta_image, @garden.photos.first.url %>

<%= render partial: "shared/show_garden", locals: { garden: @garden, markdown: @markdown } %>
