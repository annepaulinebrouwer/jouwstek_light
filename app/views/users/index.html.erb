<h1> Users: </h1>
</div>

  <div class="container">
      <% @users.each do |user| %>
        <%=  link_to user.fullname, user_path(user) %>
        <p><%= user.description %></p>
        <p><%= user.photo %></p>
      <% end %>
    </div>
